<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.atividadedois">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".University_Activity"></activity>
        <activity
            android:name=".Diamond_two_Activity"
            android:theme="@style/AppDiamond" />
        <activity android:name=".Diamond_Activity" />
        <activity android:name=".Whats_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Looki_Activity" />
        <activity
            android:name=".Blue_Activity"
            android:theme="@style/AppThemeTwo" />
        <activity android:name=".AdventureActivity" />
        <activity android:name=".City_Activity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>